# 2026-09-24 작업 기록 — Phase 0 파일 분리 + Phase 1 토큰 통합

원본: `에니어그램_통합_기능확장_로컬프로토타입.html` (2.1MB 단일 파일)

## 1. 파일 분리 (화면 변화 없음)

- `<style>` 29개 → `css/layers/00~28-*.css` (원래 id 이름과 로드 순서를 그대로 유지)
- 인라인 `<script>` 8개 → `js/00~07-*.js`
- 핸드북 데이터(1.1MB) → `content/handbook-type-store.js`. 기존 코드가 읽는 `#handbookTypeStore` 요소를 같은 위치에 만들어 주므로 JS 수정 없이 동작한다. `file://`로 더블클릭해서 열어도 동작한다.
- `index.html`: 2.1MB → 346KB
- 검증: 11개 화면 × 5개 뷰포트 = 55장 모두 원본과 픽셀 동일

## 2. 디자인 토큰 통합

`css/tokens.css`를 새로 만들어 가장 먼저 로드하고, 모든 레이어의 하드코딩 값을 토큰으로 치환했다.

| 항목 | 전 | 후 |
|---|---|---|
| Hex 색상 사용 | 1,810회 | 131회 (1,661회 치환) |
| font-size 종류 | 48종 (9.5, 12.5, 13.5px 등) | 토큰 9단계 |
| font-weight 종류 | 18종 (650, 750, 850…) | 5종 (400–800) |
| font-family | 14종 (Pretendard 혼용) | `var(--font-sans)` 1종 |
| border-radius px | 356회 / 26종 | 토큰 5단계 |
| box-shadow | 46종 | sm / md / lg |
| z-index | 20종 (최대 1200) | 토큰 7단계 |
| 중복 정의된 토큰 이름 | 24곳 | 0 (`tokens.css`만 정의) |

### 치환 규칙

- 색상은 속성 맥락(글자 / 배경 / 테두리)과 명도·채도를 보고 의미 토큰에 매핑했다.
  - 예전 테마에서 이름은 `--blue-*`인데 값은 보라(#F4F0FA, #74479F 등)였던 변수가 많았고, 이것들을 Primary 계열로 통일했다.
- 9개 유형 색은 `--type-1` ~ `--type-9`(+`-soft`) 토큰으로 옮겼고 값은 그대로 유지했다.
- font-size는 가장 가까운 단계로 옮겼다. 15px 본문은 가이드 §6A.3에 따라 16px로 올렸다. 12px 미만(9–11px)은 12px로 올렸다.
- Hero·Display 제목은 가이드의 Display 40px에 맞춰 `clamp(32px, 4vw, 40px)`로 바꿨다. 기존 최대값은 약 76px였다.
- `@media print` 블록은 건드리지 않았다.

### 남은 하드코딩 색 (131회)

대부분 예전 다크 사이드바 테마의 잔재(#334155, #16243A 배경, #D8E3F1 글자 등)와 핸드북 경고 박스의 베이지·주황 톤이다. 의미를 확인해야 해서 자동 치환하지 않았다. 목록은 `docs/token-report.json`에 있다.

## 3. 같이 고친 버그 (원본에도 있던 문제)

1. **모바일·태블릿 메뉴 글자가 세로로 쪼개짐**: 1000px 이하에서 메뉴 버튼을 `30px 아이콘 칸 | 텍스트 | 화살표` 3칸으로 잡았는데, 실제로는 아이콘 요소가 없어서 메뉴명이 30px 칸에 들어가 한 글자씩 줄바꿈되고 있었다. → 2칸(`1fr 32px`)으로 수정 (`23-codex-mobile-blue-refinement.css`)
2. **검색 레이어가 헤더 아래에 깔림**: 검색 레이어는 z-index 120, 헤더는 1000이었다. → 검색 `--z-modal`(500), 헤더 `--z-dropdown`(200)

## 4. 이번에 손대지 않은 것

- `!important` 5,551개, breakpoint 39종 → Phase 2·4에서 정리
- 콘텐츠, 문구, 메뉴 구조, 기능, JS 로직
- 여백(padding / margin)의 임의 값 → Phase 4
- 홈 Hero 다이어그램에서 '번호보다 패턴' 카드가 3번 노드를 가리는 문제(원본과 동일) → Phase 3에서 처리

---

# 2026-09-24 (2) — IA 개편: GNB 1Depth · 2Depth 고정 탭 · 유형 체크 흐름 · 홈 CTA

요청: GNB hover 메뉴 제거, 2Depth는 탭으로 고정 노출, 유형 체크는 메뉴에 펼치지 않고 간편/정식 CTA로 진입, 홈 CTA 2개.

## 바뀐 것

- **GNB**: 1Depth 버튼만 남기고 hover mega 메뉴·chevron 마크업 삭제.
- **2Depth 공통 탭** `#pageSubnav` (헤더 바로 아래, `.shell-app` flex 흐름 안): 사이드바 세부 메뉴 버튼을 원본으로 렌더링(`renderPageSubnav`). 이해·핸드북·비교·돌아보기·나의 공간에 노출, 홈·유형 체크에는 없음. 예전 퀵메뉴(`#pageRemote`), 핸드북 1~9번 탭(03 스크립트가 만들던 것), 나의 공간 `.myspace-tabs`는 이 탭으로 대체해 삭제.
- **유형 체크 흐름** (`showCheckTarget`): `start`(간편 검사하기 / 정식 검사하기) > `quick` 또는 `select`(헷갈리는 유형 여러 개 선택, 9개 모두 선택 가능, 간편 결과가 있으면 미리 선택) > `detail-N`(선택한 유형만, 진행 단계 + 이전/다음) > `result`(선택·완료 유형 카드만).
  - 해시: `#check` 시작, `#check-quick`, `#check-select`, `#check-detail-N`, `#check-result`
  - 사이드바 '유형 체크' 하위 11개 항목 삭제, 03 스크립트의 '내 유형 진단하기' 패널 삭제
- **localStorage**: `enneagram_detail_selection` 새 키 `{schemaVersion:1, types:[..], updatedAt}`. 예전 `enneagramSelectedDiagnosisType`(단일 번호)이 있으면 `types:[n]`으로 옮긴다. 기존 응답 키(`enneagram_types_1_to_9_answers_v2`, `enneagram_quick_check_v2`)는 그대로.
- **홈**: Hero·하단 CTA를 '나의 유형 찾기'(유형 체크 시작) / '내 결과 보기'(정식 결과 > 간편 결과 순) 2개로 통일. 결과가 없으면 두 번째 버튼은 '9가지 유형 둘러보기'. 결과·내 유형·성찰 기록이 있을 때만 '이어서 보기' 섹션 노출. Start here·9 Types·Patterns 섹션에 해당 메뉴로 가는 '전체 보기' 링크.
- 문구: '시간을 대하는 법' > '시간을 대하는 나' (다른 8개 항목과 형태 통일)
- 스타일은 `28-codex-round6-md-harness-execution.css` 끝 'IA 개편' 블록에 토큰만 써서 추가 (새 Hex·`!important` 없음, breakpoint 767 / 768–1023만 사용).

## 같이 고친 버그

- **정식 검사 문항·결과 카드가 두 벌로 그려짐**: 파일 분리 때 index.html에 이미 렌더링된 135문항과 결과 카드가 남아 있었는데, JS가 같은 id로 한 번 더 그려서 유형 하나를 열면 같은 문항이 두 번 보였다. → 렌더링 전에 `#app`, `#resultGrid`를 비운다 (index.html의 정적 사본 삭제는 Phase 4).
- 모바일 드로어에서 하위 메뉴가 없는 항목을 눌러도 드로어가 다시 열리던 문제 → 하위 메뉴가 있을 때만 다시 연다.

## 남은 것

- 레이어 14·16·23·24·28에 남은 `.top-mega*`, `.top-submenu-btn`, `.page-remote*` 규칙은 이제 쓰이지 않음 → Phase 4에서 삭제
- 정식 검사 헤더 문구('에니어그램 1~9번 유형 체크 · 총 135문항')는 그대로 둠

---

# 2026-09-24 (3) — 유형별 핸드북 공통 스타일 맞춤

- 상단 간격: 2Depth 탭 아래 16px > 다른 메뉴와 같은 72px(모바일 56px). `--harness-page-top` (28 레이어 :root, 토큰 alias)
- 페이지 전체 흰 배경 제거: `.type-page`의 surface 배경(24 레이어) 삭제 > 다른 메뉴처럼 연한 페이지 배경 위에 흰 카드
- 영역 바로가기 탭(01 유형 이해 ~ 04 성장), 빈 대표 이미지 카드 삭제. 소제목 번호 배지의 인라인 보라색(#8458B3) 제거 > Primary
  - 셋 다 핸드북 데이터(gzip)에 박혀 있어 `normalizeMountedPages`에서 렌더링 때 정리. 데이터 원본은 그대로.
- 쓰지 않게 된 `js/01-handbook-section-nav-behavior.js` 삭제
- 모바일(767px 이하) 상단 카드: 목록·태그가 번호 상자 폭만큼 들여쓰이던 문제 → 27 레이어의 본문 2번째 칸 고정 규칙을 768px 이상에만 적용. 모바일은 제목만 번호 옆, 목록·태그는 카드 전체 폭

---

# 2026-09-24 (4) — 나를 돌아보기 공통 레이아웃

- 경로 줄(Enneagram · 나를 돌아보기, 숨겨져 있던 A−/A+ 포함)과 3단계 안내 카드 삭제. 글자 크기 버튼 JS도 삭제
- 본문 폭 900px 제한(08 레이어) 제거 > 다른 메뉴와 같은 1120px 컨테이너
- 상단 간격: 핸드북과 같은 `--harness-page-top` (72px / 모바일 56px). 25 레이어의 핸드북 wrap 예외에 sharing wrap 추가, 08 레이어의 wrap padding-top 규칙 삭제

---

# 2026-09-24 (5) — 홈 버그·검색 아이콘·상단 여백

- 버그: CSS `content:"&gt;"`가 글자 그대로 보이던 문제(홈 유형 카드, 핸드북 자동 패턴 단계) > `">"`. 홈 카드 화살표 색도 토큰(`--color-text-tertiary`)으로
- 푸터의 'Home prototype · v0.1' 삭제
- 검색 버튼: 글자 > 돋보기 SVG 아이콘(40×40, aria-label·title '검색'). 모바일 전용 '⌕' 글자 규칙 삭제
- 홈 Hero 상단 여백 92/64/42px > `--harness-page-top` (72px / 모바일 56px)

# 2026-09-24 (6) — 홈 배경 · 9유형 색 통일 · 프로필 카드 영역

요청: 홈 구조는 기존 그대로, 뒤에 배경 그림 추가, 1~9 유형 색 통일, 9개 프로필 카드 영역 추가. (중간에 시도한 카드 덱 첫 화면은 되돌림)

- **Hero 배경**: 이미지 파일 없이 인라인 SVG `.home-hero-bg` (은은한 원 · 흰 궤도선 · 점선 · 작은 구슬 · 점). 색은 --color-primary / --color-primary-soft / --color-surface만 사용, aria-hidden.
- **9유형 색 통일**: 17 레이어의 유형별 9색 대신 --color-primary 하나 (28 레이어에서 --type-color 변수만 덮어씀, !important 없음).
- **9 Profiles 섹션 추가** (`#home-profiles`, 9가지 유형 섹션 바로 아래): 번호 · 이름 · 대표 말투 · 한 줄 설명 · #키워드 3개, 누르면 핸드북. 모바일은 옆으로 넘기는 한 줄.
- **나의 프로필**: 내가 고른 유형 > 정식 검사 최고 점수 > 간편 검사 결과 순. 해당 카드에 '나의 프로필' 배지 + 메타(출처 · 중심 · 호니비언 · 하모닉), 섹션 위에 '핸드북 보기 / 카드 이미지로 저장'(canvas 1080×1350 PNG).
- 대표 말투 · 키워드는 초안 — js/00-app-core.js의 HOME_PROFILES에서 수정.
- 확인: Python이 없어 tools/visual-check.py는 실행 못 함. 브라우저로 390 / 1440에서 가로 스크롤 · 콘솔 오류 없음 확인.

# 2026-09-24 (7) — 홈 Hero 정리 · 배경 전체 폭

요청: '9가지 유형을 한눈에 보기' 삭제, 우측 에니어그램 카드 삭제, 배경은 잘리지 않고 전체로, 더 화려하게.

- **삭제**: `#home-types` 섹션(9가지 유형 한눈에 보기), Hero 우측 `.hero-visual`(도형 카드 + 설명 말풍선 2개). '9가지 유형 둘러보기' 버튼과 결과 없을 때 이동 위치는 `#home-profiles`로 변경. 쓰지 않게 된 유형 카드 색 통일 규칙도 삭제.
- **배경 전체 폭**: `.home-hero-bg`를 `#home-top`(1120px) 밖으로 펼침(`width:100vw`, 20 레이어의 전역 `svg{max-width:100%}`는 이 요소만 `max-width:none`으로 해제). Hero는 `overflow:visible`, 배경 아래쪽은 mask로 서서히 사라짐. 데스크톱 Hero 최소 높이 640px(`--space-10` × 8).
- **더 화려하게**: 흐린 빛 번짐, 큰 에니어그램 도형(선 그라디언트, 빛나는 노드), 흰 궤도선, 천천히 도는 점선 궤도(120s, `prefers-reduced-motion`이면 정지), 기울어진 타원 궤도, 별자리 점선, 구슬, 반짝이 별. 색은 --color-primary / --color-primary-soft / --color-primary-hover / --color-surface만 사용.
- 모바일(≤767): 글과 겹치므로 도형은 옅게(35%), 제목 위에 걸리는 큰 구슬은 숨김.
- 확인: 1650 / 1024 / 390에서 가로 스크롤 없음, 콘솔 오류 없음.

# 2026-09-24 (8) — 홈 상단을 어두운 배너로 · 큰 화면에서 도형 크기 고정

요청: 전체 화면에서 도형이 너무 큼, 상단 영역을 어두운 색 배너로 확실히 구분.

- **토큰 추가** (`tokens.css` Banner): `--color-banner` #13254A, `--color-banner-deep` #0B1733, `--color-on-banner` #FFFFFF, `--color-on-banner-muted` #C5D3EC(대비 약 10:1), `--color-banner-accent` #8DB0F5(대비 약 7:1).
- **구조**: `.home-hero-bg`는 화면 전체 폭 div(배너 배경: deep > banner > primary-hover 그라디언트, 아래 경계는 또렷하게), 그 안의 그림 `.home-hero-art` SVG는 가운데 최대 1600px. 화면이 넓어져도 그림이 커지지 않음(1920에서도 1440과 같은 크기). 아래쪽 흐려짐(mask) 제거.
- **배너 위 글자**: 제목 흰색, 강조 줄 banner-accent, 본문 on-banner-muted, eyebrow 반투명 흰 배경. 16 레이어가 h1에 `color:var(--ds-text) !important`를 걸어두어 `#page-home .hero h1`에서 `--ds-text`만 바꿈(hero 전체에 걸면 같은 변수를 쓰는 흰 secondary 버튼 글자도 흰색이 되므로 h1에만).
- **그림 색**: 어두운 배경에 맞게 선·점·궤도는 banner-accent, 노드는 banner 채움 + 흰 숫자, 빛 번짐은 primary.
- 확인: 1920 / 1440 / 390에서 가로 스크롤 없음, 콘솔 오류 없음.
- **배너 아래 여백**: '이어서 보기'(`.home-continue`)가 위아래 여백 0이라 배너에 딱 붙어 보였음 > 위 여백 64px(`--space-9`). 아래 간격은 다음 섹션 위 여백이 맡으므로 0 유지. 모바일은 20 레이어가 이미 52px로 강제.

# 2026-09-24 (9) — 유형별 핸드북: 요약 먼저 → 장 탭 → 소주제 칩 (Jira SCRUM-64)

요청: 한 번에 너무 많은 정보가 보여 읽기 싫음 > 처음엔 간단한 요약, 궁금하면 더 보기. 접이식은 쓰지 않음(A안: 소주제 칩). 이후 방향: 내 상태에 맞는 다음 성장 단계(힘들 때 최악을 면하는 법, 더 건강해지는 법, 고민 분야 조언).

- **탭** `[요약 | 유형 이해 | 핵심 패턴 | 실제 모습 | 성장]`, 기본은 요약. 탭 줄은 스크롤해도 위에 붙음. 주소 `#handbook-3-real`처럼 탭까지 바로 열림.
- **장 탭 안**: 소주제 칩 5개씩, 한 번에 한 주제 + 아래 이전/다음(장 끝에서는 다음 장으로). 소주제는 h3 제목으로 나눔 — 9개 유형 구조가 같아 전부 적용. 본문 HTML은 지우지 않고 위치만 옮김(원래 머리말은 '유형 이해 > 기본 특징' 맨 위). 한 유형 페이지 높이 약 14,800px > 요약 약 2,300px.
- **요약(9개 유형 모두 자동)**: 본문에 이미 있는 내용에서 뽑음 — 핵심 두려움·욕망(core), 반복되는 생각·잃어버린 메시지(한눈에 보기), 멋진 점·주의할 점(장점과 주의점), 빛나는 환경(잘 맞기 쉬운 업무 환경), 연결된 유형(날개 2개 + 통합·비통합 방향), 한 줄 요약(삶을 움직이는 핵심 주제). 한 줄 설명·키워드·대표 말투는 홈 `HOME_PROFILES`. '함께하면 좋은 유형'은 근거가 없어 넣지 않음. 문장 다듬기는 SCRUM-71.
- **요즘 나는 어떤가요?**: 힘들어요 > '최악을 면하려면'(스트레스 방향 + 위험 신호 3개 + 임상 진단이 아니라는 안내), 그럭저럭 > '자동 패턴 알아차리기'(일깨우는 신호), 괜찮아요 > '더 건강해지려면'(통합 방향 + 성장 행동 3개). 선택은 저장하지 않음(저장·기록은 SCRUM-67).
- **고민되는 분야**: 소통과 관계 / 선택과 생활·시간과 돈 / 연애와 가족 / 일과 커리어 / 나에게 해줄 말 > 해당 탭·주제로 이동. **더 알아보기**: 장 카드 4개.
- **검색**: 핸드북 색인을 innerText > textContent로 바꿔 숨은 탭 내용도 찾음. 결과를 누르면 `revealHandbookAnchor`가 해당 탭을 열고 스크롤.
- **레거시 대응(새 !important 없이)**: 핸드북 레이어가 button·p·li·svg에 !important를 걸어둠 > 새 요소는 div/span 사용, 버튼은 레거시가 읽는 변수(--primary·--accent·--line·--radius-control 등)를 요소 안에서만 바꿈, 9점 도형은 svg 대신 CSS 마스크 + 번호 원, 탭 밑줄은 ::after.
- 확인: 1440 / 390 가로 스크롤 없음, 콘솔 오류 없음, 1·3·5·9번 요약 생성, 탭 주소 새로고침, 검색 '리더십' > 실제 모습 탭 열림.

# 2026-09-24 (10) — 핸드북 다듬기: 간격 · 불릿 · 카드 깨짐 · 장 탭 모양 통일 · 번호 삭제

- **위 간격**: 유형 번호 탭과 요약 탭 사이 약 100px > 약 16px. type-page의 padding-top이 `var(--harness-page-top) !important`라 이 페이지에서만 변수 값을 `--space-2`로 바꿈. 탭 위 '3번 · 성취자' 제목 삭제.
- **불릿**: 요약 목록 앞의 ✓ / ! > 둥근 점(좋은 점 Primary, 주의할 점 Warning).
- **'더 알아보기' 카드 깨짐**: 레거시가 핸드북 button 높이를 33~36px로 고정 > 새 버튼들만 `height:auto`.
- **장 탭 모양 통일**: 소주제 안의 블록을 요약과 같은 흰 카드(테두리·radius-lg, 그림자 없음)로, 안쪽 상자는 옅은 회색 한 가지(surface-subtle), 형광펜 강조 제거(굵게만).
- **번호 삭제**: 1.1 · 3.4.4 같은 소제목 번호 배지 제거. 장 머리(01 · 영문 라벨 · 장 제목)는 탭 이름과 겹쳐 빼고, 장 설명 한 문장만 탭 맨 위 소개로 남김(레거시가 장 머리 grid를 !important로 잠가 번호만 빼면 깨짐 > 요소를 교체).
- 확인: 1440 / 390 가로 스크롤 없음, 콘솔 오류 없음.

# 2026-09-24 (11) — 메뉴 통합: 유형별 핸드북 + 전체 유형 비교 + 나를 돌아보기 → '유형 탐구' (Jira SCRUM-73)

요청: 세 메뉴를 합치되 양쪽 내용이 빠지지 않게.

- **GNB** 7개 > 5개: 홈 · 에니어그램 이해 · 유형 체크 · **유형 탐구** · 나의 공간. 핸드북·비교·돌아보기 화면 모두 '유형 탐구'가 켜짐.
- **2Depth(세 화면 공통)**: 1번 … 9번 · 9유형 비교 · 유형 없이 돌아보기. 사이드 메뉴(모바일 드로어)도 같은 구성.
- **9유형 비교 / 유형 없이 돌아보기**: 기존 화면 그대로. 세부 화면(비교 7개 · 주제 9개)은 화면 맨 위 칩으로(`renderExploreChips`). 원본 사이드 메뉴 묶음(#compareGroup, #sharingGroup)은 칩의 원본으로만 남기고 숨김 — 레거시 `.shell-menu-group{display:block !important}` 때문에 클래스를 `shell-menu-source`로 바꿈.
- **유형 페이지 '실제 모습' > '삶의 장면'**: 칩 9개 = 나를 돌아보기 주제(일상·사람 사이·사랑·가족·일·돈·이끄는·쉬고 즐기는·시간). 각 칩 = ① 그 유형의 핸드북 내용(`HB_LIFE_MAP`으로 소제목 배치, '시간과 돈'은 시간/돈 카드로 나눔) ② 나를 돌아보기 6단계 전체(유형 미리 선택, 9개 유형 버튼으로 바꿔 보기 가능).
- **'MBTI와 함께 보기'** > '유형 이해' 마지막 칩으로 이동. 요약의 '고민되는 분야' 버튼은 삶의 장면 칩으로 연결.
- **나를 돌아보기 모듈**: `render()`의 템플릿을 `topicHTML(t, sel, embedded)`로 분리하고 `#page-sharing.__topicHTML(i,n)`, `__typeDetailHTML(i,n)` 공개. 기존 페이지 동작은 그대로.
- **주소**: `#handbook-N-life` 추가, 예전 `#handbook-N-real`은 삶의 장면으로. `#compare-…`, `#sharing-N` 그대로.
- **이름**: 모바일 제목·검색 분류를 '유형 탐구 · …'로.
- **빠짐 확인(스크립트, 9개 유형)**: 원본 핸드북의 모든 li·p·h3·h4·td·strong 문장이 배치 후 페이지에 있는지 비교 > 본문 누락 0. 사라진 것은 '소통과 관계 / 선택과 생활 / 가까운 관계 / 일과 커리어' 묶음 제목 4개뿐(칩이 대신함). 돌아보기: 9개 유형 페이지 × 9개 주제 모두 들어가고, 유형별 설명이 원본 함수 결과와 일치, 유형 버튼 81개씩.
- 확인: 1440 / 390 가로 스크롤 없음, 콘솔 오류 없음, 예전 주소 동작.

# 2026-09-24 (12) — 유형 탐구 탭 정리 · 나의 공간 한 페이지 · 라이브러리/커뮤니티/AI 유형 탐색 삭제

요청: '9유형 비교' > '전체 유형'(첫 탭), '유형 없이 돌아보기'는 나의 공간 성찰 기록으로, 나의 공간은 탭 없이 한 페이지·대시보드는 작게, 커뮤니티·라이브러리·AI 유형 탐색 삭제.

- **유형 탐구 2Depth**: 전체 유형 · 1번 … 9번 (전체 유형이 첫 탭). '유형 없이 돌아보기' 탭 삭제.
- **나를 돌아보기(삶의 장면 9개 주제)**: 나의 공간 > 성찰 기록 맨 위 '삶의 장면으로 돌아보기' 버튼 9개에서 엶. 돌아보기 화면은 GNB '나의 공간' 소속, 2Depth 없음, 칩 맨 앞에 '‹ 성찰 기록'. 유형 페이지 '삶의 장면' 안의 돌아보기는 그대로.
- **나의 공간**: 2Depth·사이드 세부 메뉴 삭제, 한 페이지(작은 대시보드 > 성찰 기록). 대시보드는 숫자 3개(모바일도 한 줄) + 반복 단서 · 흐름 2칸. '최근 성찰'은 아래 성찰 기록 목록과 겹쳐 삭제, '저장한 자료'(라이브러리 북마크) 삭제. `showMySpaceSection(key)`는 해당 영역으로 스크롤만(예전 #myspace-ai / -library / -community 주소는 맨 위로).
- **삭제**: 라이브러리(자료 목록·북마크), 커뮤니티(글쓰기·피드), AI 유형 탐색(두 유형 비교 대화) — 화면·스크립트 모두. 브라우저에 이미 저장된 북마크·커뮤니티 글 값은 지우지 않음(더 읽지 않을 뿐).
- 확인: 1440 / 390 가로 스크롤 없음, 페이지 이동 전체에서 오류 없음, 돌아보기 > 성찰 기록 복귀 스크롤.
- 참고: main으로 이동하면서 git이 파일 줄바꿈을 CRLF로 바꿔 둠 — 편집 스크립트는 CRLF를 유지하도록 처리.

# 2026-09-24 (13) — 다이어리를 상단 메뉴로 분리

요청: 다이어리를 새 메뉴로, 상단으로 분리.

- **GNB**: 홈 · 에니어그램 이해 · 유형 체크 · 유형 탐구 · **다이어리** · 나의 공간. 사이드 메뉴(모바일 드로어)도 같은 순서.
- **다이어리 페이지** `#page-diary` (주소 `#diary`): 나의 공간에 있던 성찰 기록 영역을 그대로 옮김 — 삶의 장면으로 돌아보기(9개 주제) · 다이어리 + 5 Why 작성 · 나의 성찰 기록 목록. 문구는 머리말(제목 '다이어리' + 소개 한 줄)만 새로 씀.
- **나의 공간**: 작은 대시보드만. '기록 추가하기 >'와 예전 `#myspace-reflection`, 홈 '이어서 보기'의 성찰 기록 링크는 다이어리로(`showDiaryPage`).
- **돌아보기 화면**: GNB '다이어리' 소속, 칩 맨 앞 '‹ 다이어리'. 모바일 제목·검색 분류 '다이어리 · 돌아보기'. 검색은 다이어리의 성찰 기록도 색인.
- **스타일**: 레거시가 `#page-myspace`에만 준 배경·폭·제목·입력칸 스타일을 `#page-diary`에 토큰으로 맞춤(새 !important 없음).
- 확인: 1440 / 390 가로 스크롤 없음, 오류 없음, 기록 저장 > 목록·대시보드 숫자 반영, 다이어리 ↔ 돌아보기 ↔ 나의 공간 이동.

# 2026-09-24 (14) — 다이어리 개선: 한 단계씩 쓰는 기록 + 날짜별 기록

요청: 다이어리가 너무 별로 > 더 깊게(질문을 끝까지 한 단계씩), 삶의 장면 버튼은 다이어리에서 빼기, 감정도 저장.

- **문제였던 점**: 입력칸 10개가 한 화면에 한꺼번에(설문지 느낌), 기록 목록은 오른쪽 좁은 칸, 삶의 장면 버튼과 5 Why가 역할이 겹침, 날짜·감정 없음.
- **새 기록(위)**: 한 화면에 질문 하나, 12단계 — 장면(+ 어떤 장면: 일상·사람 사이·사랑·가족·일·돈·이끄는 나·쉼·취미) > 감정(여러 개) > 나의 반응 > 5 Why 다섯 단계 > 가까운 단서 > 다음엔 > 제목(비우면 첫 문장) > 정리(모든 답 + 로컬 요약) > 저장. 진행 막대·'n / 12'·이전/다음·건너뛰기(첫 단계만 필수, 비우면 안내 문구), Ctrl+Enter로 다음. 질문 문구는 기존 그대로, 감정·제목 단계만 새로 씀.
- **나의 기록(아래)**: 장면별 필터, 두 번 이상 나온 단서·감정 요약, 날짜별 타임라인 카드(제목 · 감정 · 장면 · 가장 깊은 이유 · 다음엔 · 단서, '자세히'로 모든 답, 삭제). 위 입력 카드와 같은 폭(--readable-lg).
- **삭제**: 다이어리의 '삶의 장면으로 돌아보기' 버튼 9개(같은 내용은 유형 탐구 > 삶의 장면에 그대로 있음), 예전 입력 폼 마크업.
- **저장 형식**: 키·배열 구조는 그대로(`enneagram_reflections_v1`), 기록마다 `schemaVersion:2` + `emotions` 추가. 예전 기록은 `readReflections()`가 읽을 때 `emotions:[]`를 붙여 v2로 옮겨 저장 — 홈 '이어서 보기'·대시보드는 그대로 동작.
- 확인: 예전 형식 기록 옮김, 12단계 끝까지 저장, 필수 안내, 필터·요약·자세히·삭제, 1440 / 390 가로 스크롤·오류 없음. 확인용 기록은 원래대로 되돌림.

# 2026-09-24 (15) — 성장 실험 · 나의 변화 보기 · 링크로 공유/비교 (Jira SCRUM-48, 47, 76)

요청: 콘텐츠 사이트 느낌을 줄이는 기능 3개(서버 비용 없이). 로그인(카카오·구글)과 무료 DB는 나중에 — Supabase 추천(두 로그인 모두 무료 요금제에서 지원), SCRUM-57에 기록.

- **성장 실험** (다이어리, 새 기록과 나의 기록 사이): 다이어리의 '다음엔'이 자동으로 모이고 직접 추가도 가능. 동그라미를 눌러 '해봤어요' 표시 → 한 줄 소감 입력. 필터 해볼 것 / 해봤어요 / 전체, 직접 추가한 것만 삭제. 저장 키 `enneagram_experiments_v1` (schemaVersion 1: `status`{기록 id: done·note·updatedAt}, `custom`[]). 기록 자체는 건드리지 않음.
- **나의 변화 보기** (나의 공간): 제목 '나의 변화', 기간 최근 30일 / 전체, 숫자 3개(기록 · 연속 기록 일수 · 성장 실험 해봤어요 n/m), 기록한 날(최근 5주 달력, 한 가지 색 진하기 0 · 1 · 2개 이상, 오늘 테두리), 주별 기록(최근 8주 막대 + 숫자), 자주 느낀 감정 · 자주 나온 단서 · 장면별 기록(상위 5개 가로 막대 + 숫자), 흐름 요약 문장(조사 을/를 · 과/와 · 이라는/라는 자동). 그래프는 모두 개수만 보여줘서 한 가지 색(Primary)만 사용.
- **링크로 공유 · 비교** (새 페이지 `#page-share`, 메뉴에는 없음): 들어가는 곳 — 홈 '나의 프로필' 줄 '링크로 공유', 나의 공간 '내 유형 링크로 공유'. 유형 + 이름(선택, 12자) → `#share=<base64url JSON {v,t,n}>` 링크 생성, 복사 · 휴대폰 공유 창 · 받는 사람 화면 미리 보기. 링크에는 유형 번호와 이름만(안내 문구). 받은 사람: 상대 카드 → 내 유형(저장돼 있으면 미리 선택) 골라 비교 — 힘들 때 먼저 커지는 것(중심) · 문제 앞에서(호니비언) · 힘든 감정 앞에서(하모닉) 각각 두 사람 설명 + 같음/다름 안내, 핵심 두려움 · 욕망, '대화할 때' · '이렇게 말해보세요'(핸드북 성장 대화에서, 유형마다 카드 모양이 달라 둘 다 읽음). 궁합 점수 없음. 잘못된 링크는 공유하기 화면으로.
- 핸드북 원문을 다른 화면에서 읽도록 `window.getHandbookTypeHTML(n)` 공개.
- 확인: 샘플 기록 5개로 집계 · 연속 · 달력 · 주별 · 기간 전환, 실험 추가(빈 값 안내) · 체크 · 소감 · 대시보드 반영, 링크 생성 · 해석 · 비교 · 이름 없음(친구는) · 잘못된 링크, 9개 유형 모두 비교 문구 있음, 1440 / 390 가로 스크롤 · 오류 없음. 샘플 데이터는 원래대로 되돌림.

# 2026-09-25 (16) — UI/UX 통일 · 지침 문서 2개 · 유형 검사 결과 카드 · 다이어리 재설계 · 에니어그램 이해 그림 삭제

요청 8가지: ① 전체 UI/UX 통일 ② 리서치 md ③ UX 라이팅 md ④ 유형 체크→유형 검사, 결과는 '결과 보기'로, 마케팅용 유형 프로필 카드 ⑤ 다이어리 한 페이지 작성·페이지 폭 통일·유형별 성장 실험 추천·날짜 선택·성장 실험이 다이어리의 일부로 보이게 ⑥ 선택형 답변 + 자유 입력 ⑦ 에니어그램 이해 우측 카드 그림 삭제 ⑧ 프로젝트 PDF 참고.

- **지침 문서**: `docs/리서치_사용자와_질문.md`(페르소나·자주 묻는 질문·여정·민감 상황 원칙·자료 출처 지도), `docs/UX_라이팅_가이드.md`(톤앤매너·말투·에니어그램 특수 규칙·요소별 패턴·용어 사전·현재 문구 점검 70건). CLAUDE.md에 두 문서를 규칙으로 등록. (리서치 문서는 PDF 5개·docx 녹취를 텍스트로 뽑아 출처 쪽수까지 표기; 이미지 슬라이드인 강의자료.pdf는 발표 스크립트로 대신. UX 라이팅 가이드는 PDF를 읽지 못해 강의계획서 md와 사이트 문구가 주 출처 — PDF 기준 용어 검수는 다음 작업)
- **유형 이름 표준**: 개혁가 · 조력가 (강의계획서 md 기준). QUICK_TYPE_NAMES · TYPE_LABELS · 02 TYPES · 전체 유형 비교 카드 · 나의 공간 select에서 개혁자/조력자 → 개혁가/조력가.
- **공통 컴포넌트**(28 레이어 '공통 컴포넌트' 블록): `.ui-btn(-primary/-secondary/-ghost)` · `.ui-chip` · `.ui-card` · `.ui-page-head/.ui-kicker` · `.ui-empty` · `.ui-field`. 예전 화면의 같은 요소는 '별칭' 블록에서 같은 규칙(킥커 한글·Primary 14px, 페이지 제목 크기, 주/보조/텍스트 버튼, 알약 칩, 흰 카드 1px 테두리 16px 모서리 그림자 없음, 빈 상태). 비교·돌아보기 칩은 레거시 `#page-compare button{border-radius:var(--radius-control)!important}`를 변수(`--radius-control`)로 풀어 알약 모양.
- **킥커·라벨 한글화**: 홈(Continue→이어서 등 5), 에니어그램 이해 `ENNEAGRAM BASICS · 01`→`에니어그램 이해 · 1~6`, 나의 공간 `MY ENNEAGRAM`→나의 공간 · `MY CHANGE`→최근 흐름, 핸드북 요약 `ENNEAGRAM TYPE`→유형 요약, 유형 검사 `TYPE CHECK / FULL CHECK · STEP 1 / QUICK CHECK`→유형 검사 / 정식 검사 · 1단계 / 간편 검사, 에니어그램 이해·비교 본문의 영어 소제목 라벨 50여 개(HOW TO READ→읽는 법, KEY TERMS→핵심 용어, STEP 01→1단계, SCENE 01→장면 1, COMPARE · CORE→비교 · 핵심 동기 …).
- **문구(UX 가이드 반영)**: 유형 체크→유형 검사(전부), 정식 검사 점수 해석 5문장을 해요체·비단정("당신은 아마 N번 유형이 아닐 것이다"→"N번 유형과는 거리가 있어 보여요" 등), 나의 변화 요약 합니다체→해요체, 버튼 "링크로 공유하기 / 이미지로 저장하기 / 내 유형으로 저장하기", 다이어리 삭제 확인("이 기록을 삭제할까요? 삭제하면 되돌릴 수 없어요.").
- **유형 검사**: 문항 화면에서 점수·해석을 바로 보여주지 않고 진행(n/15)만, 15문항을 다 답하면 '결과 보기'. 결과 화면 `#checkResultCards`: 완료한 유형을 점수순으로 **유형 프로필 카드**(어두운 배너: 번호·이름·한 줄·중심/호니비언/하모닉·키워드·대표 말투 / 본문: 점수·구간·해석, 핵심 두려움·욕망·반복되는 생각, 멋진 점·주의할 점, 빛나는 환경, 연결된 유형, 한 줄 요약 / 버튼: 핸드북 보기·내 유형으로 저장하기·링크로 공유하기·이미지로 저장하기). 1위는 전체 카드, 나머지는 압축 카드. 간편 검사도 결과를 바로 보여주지 않고 '결과 보기' → 새 화면 `quick-result`(`#check-quick-result`)에 같은 카드 + '정식 검사로 확인하기'. 예전 점수 표(`#resultGrid`)는 계산용 id만 남기고 숨김, 어두운 `.summary` 배경은 클래스를 `check-summary`로 바꿔 레거시 !important에서 벗어남. 카드 데이터는 `typeProfileFromRoot()`(핸드북 원문 파싱, 핸드북 요약과 공유), `getTypeProfile(n)` 캐시. 레거시가 `#page-check h2/p` 색을 !important로 잠가 카드 안에서는 h2·p 대신 div(role=heading)를 씀.
- **다이어리 재설계**: 한 페이지 폼(날짜 칩 최근 7일 + date 입력 / 1 장면+영역 칩 / 2 감정 칩+기타 / 3 반응 칩+직접 쓰기 / 4 왜 그랬을까 5단계 — 유형별 이유 칩('내 유형' 표시, 핸드북 자동 패턴 3개 포함)+직접 쓰기 / 5 단서(내 유형 것 맨 앞) / 6 다음엔 — 핸드북 성장 행동 추천 칩+직접 쓰기 / 7 제목). 칩 선택과 직접 쓴 글은 ' · '로 합쳐 저장(형식 v2 그대로). 페이지 폭은 다른 메뉴와 같은 `--harness-container`, 오른쪽 sticky 카드 **'해보기로 한 것'**(= 성장 실험: 기록의 다음엔 + 유형별 추천 3개 '+ 추가' + 직접 추가, 해봤어요 체크·소감). 머리말에 기록→다음엔→해보기→다시 기록 흐름 설명. 아래 '나의 기록' 타임라인은 그대로. 예전 12단계 마법사 코드·CSS 삭제.
- **에니어그램 이해**: 우측 카드 그림(SVG) 6개 삭제, 머리 레이아웃 한 열.
- **홈**: 100vw 배너가 스크롤바 폭(8px)만큼 넘쳐 `#page-home{overflow-x:hidden}`.
- 확인: 1440 / 390에서 홈·이해·검사(시작·간편·간편 결과·고르기·문항·결과)·핸드북·비교·돌아보기·다이어리·나의 공간·공유 이동 시 오류 없음, 가로 스크롤 없음(홈은 hidden). 간편 검사→결과 보기→카드→내 유형으로 저장, 정식 검사 2유형 완료→결과 카드 2장(1위 전체·2위 압축), 다이어리 작성(어제 날짜·기타 감정·칩+직접 쓰기·유형별 추천)→저장→해보기로 한 것 반영→추천 추가. 확인용 데이터는 원래대로 되돌림. 새 !important 없음. 회귀 검사 스크립트는 여전히 미실행(Python 없음).
